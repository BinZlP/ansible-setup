---
# timezone
timezone: "Asia/Seoul"

# common packages for default-common
common_packages:
  - curl
  - dstat
  - htop
  - net-tools
  - rsync

# Default roles to run
roles_to_run: ['default-common','timedatectl','prometheus','node-exporter','grafana','nginx']
